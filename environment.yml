name: gene_genome
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - numpy
  - pandas
  - matplotlib
  - opencv
  - biopython
  - setuptools
  - wheel

  #sudo apt install ncbi-blast+ prodigal clustalw
